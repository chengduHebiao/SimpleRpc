server:
  port: 8988
  servlet:
    context-path: /lin
  tomcat:
    uri-encoding: UTF-8

spring:
  #数据库的配置
  datasource:
    name: dev
    url: jdbc:mysql://10.28.100.57:3306/bbd_post_loan?useUnicode=true&characterEncoding=utf-8
    username: postloan
    password: dev@bbd
    # 使用druid数据源
    #type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    filters: stat
    maxActive: 20
    initialSize: 1
    maxWait: 60000
    minIdle: 1
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxOpenPreparedStatements: 20
  jpa:
    show-sql: true
    hibernate:
      use-new-id-generator-mappings: false
  servlet:
    multipart:
       max-file-size: 16Mb
       max-request-size: 30Mb
